// packages
import styled from 'styled-components';

// global
import * as G from 'global/styles';

export const Container = styled(G.Center)`
  margin-top: 2rem;
  padding: 1rem 2rem;

  border: 0.1rem solid var(--hookers-green);
  border-radius: 1rem;

  background-color: var(--dark-slate-gray);
`;
