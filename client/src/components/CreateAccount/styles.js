// packages
import styled from 'styled-components';

// global
import * as G from 'global/styles';

export const Content = styled(G.Center)`
  padding: 1rem 2rem;

  border: 0.2rem solid var(--hookers-green);
  border-radius: 1rem;

  background-color: var(--dark-slate-gray);
`;
